# CodeForge daily-driver product R1 — continuation certification

**Verdict: BLOCKED.** Candidate `8b54f3c` packages and passes deterministic desktop smoke,
including full workflow repair, interruption/recovery and browser-boundary checks. The resumed
real profile also proves a previously failed task can no longer retain an active approval wait.

The remaining block is live evidence, not an asserted product state: the supported OpenRouter
account is at its provider's daily free-request cap. CodeForge presents that state and does not
fall back to paid OpenAI or Anthropic credentials. A fresh live completion, real failure→repair
completion and route-to-route failover must be rerun when a verified free provider is available.

Z.AI direct free discovery is now guarded by current authenticated availability plus fresh 0/0
pricing evidence. OpenAI has no supported zero-cost API route. Anthropic promotional/new-user
credits remain explicitly non-recurring and are not placed in ForgeAuto/Free.
