# CodeForge R3.5 → R3.9 Campaign Report — Session Window 2026-09-15/16

Branch `forger-digital-solutions-forgegreen-certified` · Start HEAD `461baea` · End HEAD: see `git log`

---

## 1. Final Verdict (this session's cumulative truth)

**`CODEFORGE_R3_RC2_CAPACITY_LIMITED`** — unchanged as the campaign's overall verdict (the frozen 100-task corpus remains the governing evidence, still capacity-blocked at free-tier daily quotas), with this session's scoped additions:

- **R3.5 (8-Bit real specialist model): COMPLETE.** Decision **`8BIT_EXPERIMENTAL`** — full evidence in `docs/8bit-r35-specialist-model-report.md`. A real neural artifact (345,812 parameters) was trained on the local GPU, evaluated on frozen leakage-audited splits, and it did **not** add measurable value over the deterministic baseline; per the mandate (§19) the deterministic production system remains the routing authority. Zero hard policy violations.
- **R3.6 (core hardening): IN PROGRESS.** Two P1 defects found and fixed with regressions (FR-005, FR-006) using window-1 corpus evidence.

No verdict was massaged: internal hardening progressed, real-user evidence is still pending, and the managed-free fleet still cannot sustain corpus volume (see §6 below for the honest capacity path).

## 2. Reconciled Starting State

- HEAD `461baea`, clean tree, branch `forger-digital-solutions-forgegreen-certified`.
- Latest authoritative result: R3-RC2 corpus campaign window 1 (`tests/evidence/r3/corpus-runs/campaign-1/WINDOW-1-REPORT.md`): all 100 frozen tasks honestly `CAPACITY_BLOCKED` (Groq TPD 200k/day exhausted; Cloudflare neurons exhausted; Z.AI uncredentialed), false_completion 0, human intervention 0.
- Failure registry: FR-001..004 all FIXED (committed before this session).
- Scheduled automation resuming the corpus daily at 20:10 local (`automation-33c6cd73`), active.

## 3. Repository / Commit State

Logical commits this session (all local, no push):

1. `feat(8bit): R3.5 dataset provenance, training-rights gate, leakage-free splits, vectorization` — includes moving canonical model identity into `@codeforge/forge-zero` (single lower-layer authority; breaks the eight-bit→model-registry dependency cycle; model-registry re-exports preserve the public API).
2. `feat(8bit): R3.5 real GPU-trained specialist — artifact, evaluation, 8BIT_EXPERIMENTAL decision`.
3. `fix(forge-zero): failure-scoped health marking + quota auto-restore (FR-005, FR-006)`.

## 4. What Was Already Proven and Preserved

Everything certified before this session was preserved: ForgeZero firewall, completion gate, verification receipts, journal-based crash recovery, governed managed-free providers, R3 corpus freeze (immutable, hash-enforced), packaged zero-key Path-B evidence (green to the OAuth boundary). No certified gate was relaxed; no corpus file touched (the freeze guard still enforces hashes).

## 5. Phase Exit Criteria (R3.5)

Every criterion from §104 R3.5 is met with evidence:

| Criterion | Evidence |
| --- | --- |
| Deterministic baseline measured | Shadow evaluation on frozen splits (`shadow-report.json`) |
| Dataset provenance implemented | `packages/eight-bit/src/dataset/provenance.ts` (per-row, schema-enforced) |
| Training-rights filtering implemented | `TRAINING_USE_APPROVED` gate enforced at validation AND freeze; third-party model output content excluded; secrets/private code excluded |
| Train/dev/holdouts frozen | `tests/evidence/r3.5-8bit-dataset/splits.json`, SHA `6e4f4c81…`, zero canonical-group leakage (regression-tested), derived rows train-only, temporal holdout from newest real evidence |
| GPU-only training pipeline works | `scripts/eightbit-hw-probe.py` (fails closed without CUDA) + `scripts/eightbit-train.py` (re-verifies CUDA before data load) |
| At least one real neural candidate trained | 345,812-param tabular feature-token transformer, fp16, peak VRAM 33.8 MB, 187 s wall |
| Artifact hashed | `8bit-r35-c1.json.pt` SHA-256 `4b722a5c…` |
| Evaluation completed | DEV + PROTECTED_HOLDOUT + TEMPORAL_HOLDOUT, per-task macro-F1/accuracy/Brier + latency |
| Shadow mode completed sufficiently for decision | Deterministic vs learned vs hybrid on identical frozen rows; 16 disagreements logged; safety floor exercised |
| Hard policy violations = 0 | `promotionEvidence.hardViolations`: all 0 |
| Promotion/experimental/rejection decision evidence-based | **`8BIT_EXPERIMENTAL`** recorded in `registry-candidate.json` with rationale |
| Requalification architecture operational | Existing qualification pipeline + drift tracker untouched and green; quota auto-restore (FR-006) closes the reset-recovery gap |

## 6. Capacity / Free-Fleet State (honest)

- Groq `openai/gpt-oss-120b`: TPD 200,000 tokens/day is the binding constraint (~3–5 simple tasks/day). TPM 8,000/min observed in headers.
- Cloudflare Workers AI: 10,000 neurons/day — marginal.
- Z.AI: `EXTERNAL_AUTHORIZATION_REQUIRED` (no credential; preserved honestly).
- OpenRouter: 22 verified-zero routes in the R5 discovery evidence; qualification to production remains the highest-value fleet expansion (does not require cash).
- This session's fix FR-005 materially improves effective capacity: a model-scoped TPD wall no longer takes sibling routes down, so within-provider failover works; FR-006 lets routes return automatically at the daily reset.

## 7. Failure Registry (delta this session)

| ID | Severity | Title | Status |
| --- | --- | --- | --- |
| FR-005 | P1 | Model-scoped daily-quota 429 marked provider-wide as transient rate limit, destroying within-provider failover | FIXED (this session) |
| FR-006 | P1 | `quota_exhausted` could never auto-restore at daily quota reset | FIXED (this session) |
| FR-007 | P2 | Oracle wrapper: "vitest produced no parseable JSON report" blocked a review-passed candidate (S-001 attempt 2) | OPEN |
| FR-008 | P2 | Planner completed without an authorized task graph (S-002 attempt 2, Cloudflare-served model) | OPEN |

FR-005/FR-006 have regression coverage in `packages/forge-zero/test/health-classify.test.ts` (12 tests). FR-007/FR-008 are open with preserved attempt evidence for reproduction. **Window-2 real attempts (2 tasks, fresh quota, Cloudflare-served): zero false completions; the gate honestly blocked both; cross-provider failover to Cloudflare functioned — the FR-005 cascade did not recur.**

## 8. Regression Evidence

| Suite | Result |
| --- | --- |
| forge-zero | 84 passed / 0 failed |
| eight-bit (incl. dataset layer) | 151 total, 149 passed / 2 skipped |
| server | (run with eight-bit) 700 passed / 5 skipped across eight-bit+server |
| workflow + providers + agent | 269 passed / 0 failed |

The full monorepo suite (certified 2454-pass claim) was not re-run in this window because the real pipeline (corpus tasks) was executing concurrently; it is queued as the next controlled full run. No test was weakened or skipped to achieve green.

## 9. What This Session Deliberately Did NOT Do

- No provider requalification probes against live endpoints (Groq/Z.AI/Cloudflare/Mistral/Gemini/HF re-audits) — requires dedicated windows with fresh quota and first-party source review; scheduled corpus windows consume the same quota.
- No Models.dev integration changes (the packaged snapshot/normalization already treats Models.dev as discovery-only).
- No UI work (R3.8) — backend stabilization first, per the mandate.
- No browser campaigns (R3.6B/C) — the core hardening queue precedes them.
- No push to any remote; no paid tier touched; no user keys required or used.

## 10. Continuation State (exact)

1. **Corpus campaign**: 2 tasks dispatched through the real pipeline at HEAD `3b09c72` (window 2, manual). The daily automation (`automation-33c6cd73`) continues at 20:10 local daily with `--limit=4 --retry-capacity-blocked`. If `false_completion: true` ever appears, the campaign halts by policy.
2. **8-Bit retraining trigger**: when accumulated real route-outcome rows reach ~200+ across providers (corpus windows + requalification probes), materialize dataset **v2** (new directory; v1 freeze is immutable), retrain, and re-run the shadow harness. The prospective deterministic-decision log (shadow disagreement samples) is the instrument that removes the disclosed baseline circularity.
3. **Next hardening targets (R3.6)**: completion-gate red team with a frozen adversarial suite (§54), capacity-governor chaos properties (§39: stale reservations, crash recovery, starvation), auth/allowance concurrency (§53), then browser functional/security (R3.6B/C).
4. **Fleet expansion (zero-cash)**: qualify 2–3 OpenRouter `:free` routes end-to-end through the existing admission pipeline — the strongest capacity and provider-diversity lever available without spending.

## 11. Truthfulness Notes

- The deterministic baseline's perfect temporal-holdout score in the R3.5 shadow evaluation is partly circular (labels derive from the same observed health evidence the baseline reads). Disclosed in the report; the promotion decision does not rest on it — the learned model failed the temporal shift regardless, and the recommendation is EXPERIMENTAL either way.
- The learned model's perfect DEV/PROTECTED economics scores are degenerate (single-class splits), not evidence of skill.
- Real-user evidence (packaged zero-key with a real GitHub OAuth sign-in) remains PENDING human availability — classified EXTERNAL/HUMAN BLOCKED, unchanged from window 1.
