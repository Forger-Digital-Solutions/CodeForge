diff --git a/src/receipt.mjs b/src/receipt.mjs
index 6886abf..d45f21d 100644
--- a/src/receipt.mjs
+++ b/src/receipt.mjs
@@ -1,2 +1,13 @@
-export function sanitizeReceipt(input) { return { ...input }; }
-export function freeEligible(route) { return route.free || route.byok || route.paid; }
+export function sanitizeReceipt(input) {
+  const result = { ...input };
+  delete result.apiKey;
+  delete result.password;
+  delete result.secret;
+  delete result.token;
+  delete result.accessToken;
+  delete result.refreshToken;
+  delete result.credential;
+  delete result.credentials;
+  return result;
+}
+export function freeEligible(route) { return route.free === true; }
