diff --git a/src/continuation.mjs b/src/continuation.mjs
index 22d2972..ddf6038 100644
--- a/src/continuation.mjs
+++ b/src/continuation.mjs
@@ -1,4 +1,7 @@
 export function applyResult(state, result) {
+  if (state.consumed.includes(result.id)) {
+    return state;
+  }
   state.writes.push(result.value);
   state.consumed.push(result.id);
   return state;
