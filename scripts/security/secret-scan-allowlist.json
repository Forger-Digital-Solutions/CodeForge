{
  "$comment": "Reviewed false positives for scripts/security/secret-scan.mjs. Each entry pins ONE path and ONE pattern type; never use a wildcard path. Add an entry only after a human confirmed the flagged text is not a credential, and say why.",
  "entries": [
    {
      "path": "scripts/security/secret-scan.mjs",
      "type": "*",
      "reason": "The scanner's own --self-test corpus contains deliberately realistic fake shapes so the gate proves it still detects them."
    },
    {
      "path": "docs/security/key-management-and-rotation.md",
      "type": "private_key_kv",
      "reason": "Line 62 names the GITHUB_APP_PRIVATE_KEY environment variable in an operator rotation procedure; it contains no key material or value."
    },
    {
      "path": "docs/security/logging-and-audit.md",
      "type": "aws_secret_env",
      "reason": "The redaction reference table (section 1) names the credential SHAPES the runtime redactor masks, e.g. `password=`, `codeforge-session=`; the cells contain field names and prefixes only, no values."
    },
    {
      "path": "docs/security/logging-and-audit.md",
      "type": "aws_secret",
      "reason": "The redaction reference table (section 1) names the credential SHAPES the runtime redactor masks, e.g. `password=`, `codeforge-session=`; the cells contain field names and prefixes only, no values."
    },
    {
      "path": "docs/security/logging-and-audit.md",
      "type": "private_key_kv",
      "reason": "The redaction reference table (section 1) names the credential SHAPES the runtime redactor masks, e.g. `password=`, `codeforge-session=`; the cells contain field names and prefixes only, no values."
    },
    {
      "path": "docs/security/logging-and-audit.md",
      "type": "password",
      "reason": "The redaction reference table (section 1) names the credential SHAPES the runtime redactor masks, e.g. `password=`, `codeforge-session=`; the cells contain field names and prefixes only, no values."
    },
    {
      "path": "docs/security/logging-and-audit.md",
      "type": "session_cookie",
      "reason": "The redaction reference table (section 1) names the credential SHAPES the runtime redactor masks, e.g. `password=`, `codeforge-session=`; the cells contain field names and prefixes only, no values."
    },
    {
      "path": "docs/security/logging-and-audit.md",
      "type": "control_plane_token",
      "reason": "The redaction reference table (section 1) names the credential SHAPES the runtime redactor masks, e.g. `password=`, `codeforge-session=`; the cells contain field names and prefixes only, no values."
    }
  ]
}
